from webexteamssdk import WebexTeamsAPI
import util

# def createTeamSpace(db, api, teamId: str):
#     teamSpaceName = util.getTeamName(db, teamId)
#     print(f"Creating team space for {teamSpaceName}")

#     createdSpace = api.rooms.create(teamSpaceName)

#     print(f"Created space {createdSpace.title} with Id: {createdSpace.id}")
